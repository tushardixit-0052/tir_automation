[pytest]
testpaths = tests
addopts = -ra -q
log_cli = true
log_cli_level = INFO
markers =
    smoke: smoke tests
    regression: regression tests
    instance: instance tests
    training_cluster: training cluster tests
    dataset: dataset tests
    sfs: shared file system tests
